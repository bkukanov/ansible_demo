#!/bin/bash
ss_user=""
ss_pwd=""
hub_user=""
hub_pwd=""

token=$(curl -s --data "username=$ss_user&password=$ss_pwd" --insecure https://$1:8443/rest/v1/identity/gettoken)
curl -X POST -k "https://$1:8443/rest/v1/hub/register?hubIp=hub.subut.ai&email=$hub_user&password=$hub_pwd&sptoken=$token"
